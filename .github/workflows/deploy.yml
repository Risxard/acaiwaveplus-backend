name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependências
        run: npm ci

      - name: Deploy para VPS (via SSH)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
<<<<<<< HEAD
          source: "dist,package*.json,Dockerfile,docker-compose.yml"
          target: "/srv/acaiwaveplus-api"

      - name: Rodar docker-compose na VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /srv/acaiwaveplus-api
            docker-compose down
            docker-compose up -d --build
=======
          source: "."
          target: "/srv/acaiwaveplus-api"
>>>>>>> 160be057cde509eb0bd3a8150070efc10b65fecf
